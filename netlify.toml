[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_CONFIG_PRODUCTION = "false"
  VITE_API_URL = "tu-url-de-api"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# La sección [context.production.environment] con NODE_ENV fue eliminada